import './App.css';
import {fetchData} from "./sevices";


fetchData().then(console.log )
const App = () => {

    return (
        <div>
            asfasf
        </div>
    );
};

export default App;